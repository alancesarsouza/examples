
import messages from './messages';

const project = '/projeto';

const routes = {
  project,
  async: `${project}/assincronas`,
};

export { routes };
export { messages };

export default {
  messages,
  routes,
};
